{
  "name": "@homestead/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no client unit tests yet — covered by sim tests and e2e)\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "@homestead/sim": "workspace:*",
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
